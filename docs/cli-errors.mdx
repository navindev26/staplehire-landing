---
title: "Staplehire CLI errors"
description: "Fix Staplehire CLI authentication, validation, permission, conflict, upstream, usage, and job timeout errors."
sidebarTitle: "CLI errors"
---

Staplehire CLI failures return structured JSON on stderr and stable exit codes so humans, scripts, and agents can recover predictably.

```json
{
  "error": {
    "name": "ValidationError",
    "code": "candidate_email_invalid",
    "message": "Invalid email address",
    "hint": "Use a valid email format",
    "field": "email",
    "requestId": "req_...",
    "status": 400
  }
}
```

## Quickstart

Run `doctor` first when a command fails unexpectedly.

```bash
staplehire doctor -q
```

Then inspect the command-specific error JSON on stderr.

```bash
staplehire candidates create <roleId> --email not-an-email 2> error.json
jq '.error' error.json
```

## Exit codes

| Code | Meaning | Typical fix |
| --- | --- | --- |
| `1` | Generic or internal error | Retry or inspect `requestId` |
| `2` | Authentication, usually `401` | Run `staplehire login` or set `STAPLEHIRE_KEY` |
| `3` | Validation, usually `400` | Fix the flagged field or missing argument |
| `4` | Not found, usually `404` | List resources and use a real ID |
| `5` | Conflict, usually `409` | Reuse the existing resource or change unique fields |
| `6` | Permission, usually `403` | Check key organization and user permissions |
| `7` | Upstream, usually `502` | Retry later or contact support with `requestId` |
| `8` | CLI usage or bad args | Run `staplehire <command> --help` or `staplehire commands` |
| `9` | Poll timeout | Re-run `jobs poll` with a longer timeout |

## AuthenticationError

The CLI could not resolve or validate an API key.

```bash
staplehire login
staplehire whoami
```

For CI and agents:

```bash
export STAPLEHIRE_KEY=sh_live_xxx
staplehire doctor -q
```

## ValidationError

A required value is missing or invalid.

```json
{
  "error": {
    "name": "ValidationError",
    "code": "candidate_email_invalid",
    "field": "email",
    "message": "Invalid email address"
  }
}
```

Fix the field named by `.error.field`, then rerun the command.

## NotFoundError

A role, candidate, design, stage, or job ID does not exist or is not visible to the current organization.

```bash
staplehire roles list
staplehire candidates list --role-id <roleId>
staplehire designs list <roleId>
staplehire jobs get <jobId>
```

## ConflictError

A resource already exists, commonly a duplicate candidate email on the same role.

```bash
staplehire candidates list --role-id <roleId> --email alex@company.com
```

## Poll timeout

Exit code `9` means `jobs poll` stopped waiting. It does not necessarily mean the job failed.

```bash
staplehire jobs poll <jobId> --interval 5000 --timeout 300000
staplehire jobs get <jobId>
```

## FAQ

### Where does error JSON print?

Errors print to stderr. Successful data prints to stdout.

### What should I send to support?

Send `.error.requestId`, the command, and the timestamp.

### Why did `jobs poll` exit with code 9?

The timeout elapsed. The job may still be running, so inspect it with `jobs get` or poll again.

Related: [Authenticate the Staplehire CLI](/authentication) · [Poll Staplehire CLI jobs](/poll-agent-jobs) · [Command reference](/commands)